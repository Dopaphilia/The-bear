# 개발 일지 - 2026년 5월 12일

## 1. 주요 구현 시스템

### **가. 게임 루프 및 규칙 시스템 (GameManager.cs)**
- **인트로 연출:** 게임 시작 시 검은 배경에 "DAY 1" 텍스트가 나타났다가 서서히 밝아지는 시퀀스 구현.
- **데일리 규칙 관리:** 다음 날로 넘어가기 위해 이행해야 하는 3가지 필수 규칙 상태값 추가.
  - `hasLitCandle` (향초 피우기)
  - `hasTakenSupplements` (영양제 먹기)
  - `hasWashedHands` (손 씻기)
- **날짜 변경 로직:** 
  - 침대 상호작용 성공 시 날짜가 증가하며 모든 규칙 상태 초기화.
  - **향초 자동 소화:** 다음 날 시작 시 씬 내의 모든 `CandleController`를 찾아 불을 끄는 기능 추가.

### **나. 통합 아이템 상호작용 (PlayerController.cs, Item.cs)**
- **Item.cs 기반 통합:** `Item` 태그와 `itemName`을 기준으로 여러 아이템(Lighter, Medicine, Candle)을 한 곳에서 관리하도록 리팩토링.
- **영양제 복용 기믹:** "Medicine" 이름의 아이템 상호작용 시 `GameManager` 상태를 업데이트 (객체는 비활성화되지 않고 유지됨).
- **싱크대(손 씻기) 연동:** 싱크대 루틴 완료 시 `hasWashedHands` 규칙 자동 완료.
- **취침 제한:** 모든 규칙을 지키지 않았을 경우 침대 상호작용 시 "아직 할 일이 남았다..." 메시지 출력 및 진행 차단.

### **다. 대화 시스템 (DialogueManager.cs)**
- **타이핑 연출:** 텍스트가 한 글자씩 출력되는 효과 구현.
- **문장 큐(Queue) 관리:** 여러 문장을 순차적으로 출력하며 클릭/E키로 넘기기 및 스킵 기능 지원.
- **인트로 대사:** 게임 시작 직후 "으으 똥마려워.." 독백이 출력되도록 설정.

---

## 2. 게임 플레이 규칙 (To-Do List)
플레이어는 매일 밤 잠들기 전 다음 사항을 반드시 이행해야 합니다:
1.  **향초 피우기:** 라이터를 획득하여 향초에 불을 붙여야 함.
2.  **영양제 복용:** 지정된 위치의 영양제와 상호작용해야 함.
3.  **손 씻기:** 화장실 싱크대에서 세수/손 씻기 루틴을 수행해야 함.

---

## 3. 유니티 에디터 설정 참고
- **태그 설정:** `Item`, `Sink`, `Bed` 태그가 정확히 할당되어야 함.
- **Dialogue UI:** `Canvas` 하위에 `DialoguePanel`과 `DialogueText(TMP)`를 배치하고 `DialogueManager`에 연결해야 함.
- **알파 값 주의:** `BlackFade` 이미지와 `DayText`는 코드에서 알파(Alpha) 값을 제어하므로, 컴포넌트가 켜져 있어도 투명도가 0이면 보이지 않음.
